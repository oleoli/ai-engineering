# Live-session guides — written for the instructor, exported to Notion,
# never committed.
guides/

# Local environment / editor cruft (the project also has its own .gitignore
# inside estimator/, this catches files at the repo root).
.DS_Store
.idea/
.vscode/
__pycache__/
.claude/
docs/*
